namespace FileTreeSearch
{
    class Program
    {
        static void Main(string[] args)
        {
            // starts the process of finding the file name, iterating through the directory and parsing the csv file
            FileSearch.FileTraversal();
        }
    }
}